'use strict';

describe('Home', () => {
  describe('GET /', () => {
    it('<200> should always return with the API server information', async () => {
      expect(true).toBe(true);
    });
  });
});
